language: ruby
cache: bundler

sudo: required
dist: trusty

group: edge
os: osx
osx_image: xcode10.1
before_install:
  - brew update

env:
  - BREWFILE=Brewfile1
  - BREWFILE=Brewfile2

services: docker

script:
- travis_retry brew bundle --file=test/$BREWFILE